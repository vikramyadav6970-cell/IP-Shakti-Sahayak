import { useReducedMotion } from 'framer-motion'

/**
 * Re-export Framer Motion's useReducedMotion for consistent usage.
 * All animation code should check this before animating.
 * See coding_conventions.md rule 7: reduce-motion must be respected.
 */
export { useReducedMotion }
