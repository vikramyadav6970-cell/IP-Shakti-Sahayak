# =============================================================================
# IP-SAKTI Sahayak — AI Layer Dependencies
# =============================================================================
# Install: pip install -r requirements.txt
# For spaCy models: python -m spacy download en_core_web_sm
# =============================================================================

# --- Core ---
python-dotenv==1.1.0

# --- LLM Providers ---
openai==1.86.0
anthropic==0.52.0
google-generativeai==0.8.5

# --- Embeddings ---
sentence-transformers==3.4.1
torch==2.7.1

# --- Vector Store (Qdrant Cloud) ---
qdrant-client==1.14.2

# --- Keyword Search (BM25) ---
rank-bm25==0.2.2

# --- Reranker ---
# Local BGE cross-encoder reranker (default)
# Cohere rerank used if COHERE_API_KEY is set
cohere==5.15.0

# --- Database (Supabase Postgres) ---
sqlalchemy[asyncio]==2.0.41
asyncpg==0.30.0
alembic==1.15.2

# --- Cache / Broker (Upstash Redis) ---
redis==5.3.0

# --- Background Jobs ---
celery==5.5.2

# --- Document Parsing ---
PyMuPDF==1.25.5
beautifulsoup4==4.13.4
pytesseract==0.3.13

# --- Entity Extraction ---
spacy==3.8.7

# --- Document Loaders (LangChain — loaders only, not chains) ---
langchain-community==0.3.24

# --- Evaluation ---
ragas==0.2.15

# --- HTTP ---
httpx==0.28.1
aiohttp==3.12.4

# --- Utilities ---
pydantic==2.11.4
pydantic-settings==2.9.1
pyyaml==6.0.2
joblib==1.5.1

# --- Testing ---
pytest==8.4.0
pytest-asyncio==1.0.0
pytest-cov==6.2.1
